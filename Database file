CREATE DATABASE IF NOT EXISTS birds_db;
USE birds_db;

CREATE TABLE IF NOT EXISTS birds (
    Id INTEGER PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    color VARCHAR(50) NOT NULL,
    size INT NOT NULL
);

INSERT INTO birds (first_name, last_name, color, size) VALUES
('bogart', 'batumbakal', 'brown', 5),
('tweety', 'dela cruz', 'yellow', 3);


CREATE DATABASE IF NOT EXISTS cars_db;
USE cars_db;

CREATE TABLE IF NOT EXISTS cars (
    Id INTEGER PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    color VARCHAR(50) NOT NULL,
    size INT NOT NULL
);

INSERT INTO cars (first_name, last_name, color, size) VALUES
('toyota', 'corolla', 'white', 4),
('honda', 'civic', 'black', 4);


CREATE DATABASE IF NOT EXISTS books_db;
USE books_db;

CREATE TABLE IF NOT EXISTS books (
    Id INTEGER PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    color VARCHAR(50) NOT NULL,
    size INT NOT NULL
);

INSERT INTO books (first_name, last_name, color, size) VALUES
('harry', 'potter', 'red', 7),
('lord', 'rings', 'blue', 9);


CREATE DATABASE IF NOT EXISTS brands_db;
USE brands_db;

CREATE TABLE IF NOT EXISTS brands (
    Id INTEGER PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    color VARCHAR(50) NOT NULL,
    size INT NOT NULL
);

INSERT INTO brands (first_name, last_name, color, size) VALUES
('nike', 'airmax', 'white', 42),
('adidas', 'superstar', 'black', 40);


CREATE DATABASE IF NOT EXISTS clothes_db;
USE clothes_db;

CREATE TABLE IF NOT EXISTS clothes (
    Id INTEGER PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    color VARCHAR(50) NOT NULL,
    size INT NOT NULL
);

INSERT INTO clothes (first_name, last_name, color, size) VALUES
('polo', 'shirt', 'blue', 38),
('denim', 'jeans', 'black', 32);
